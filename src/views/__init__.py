from .users import bp as users_bp
from .tokens import bp as token_bp


"""
    idea:
        allow client to create a token for the client level as opposed to the user level
        They can then use the token to update their users and applications
"""